import { Button } from '@mantine/core';
import React from 'react';

const AppleRecommendChip = () => (
  <Button onClick={() => {}} radius="xl">
    애플 권장 답변
  </Button>
);

export default AppleRecommendChip;
